{
	"name": "click",
	"version": "3.0.0",
	"scripts": {
		"makepot": "wp i18n make-pot . languages/click.pot --skip-js --exclude=node_modules,deploy",
		"deploy": "npm-run-all makepot deploy:bundle",
		"deploy:bundle": "node .bin/deploy.js"
	},
	"devDependencies": {
		"archiver": "^8.0.0",
		"npm-run-all": "^4.1.5"
	}
}
